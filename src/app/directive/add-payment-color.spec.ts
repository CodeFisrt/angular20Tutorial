import { AddPaymentColor } from './add-payment-color';

describe('AddPaymentColor', () => {
  it('should create an instance', () => {
    const directive = new AddPaymentColor();
    expect(directive).toBeTruthy();
  });
});
